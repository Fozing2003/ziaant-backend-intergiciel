server.port=8082
spring.application.name=reservation-service

# Eureka
eureka.client.service-url.defaultZone=${EUREKA_URI:http://localhost:8761/eureka/}
eureka.instance.prefer-ip-address=true

# Database
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://postgresql:5432/reservation_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:ziaant}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ziaant123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# RabbitMQ
spring.rabbitmq.host=${SPRING_RABBITMQ_HOST:localhost}
spring.rabbitmq.port=5672
spring.rabbitmq.username=ziaant
spring.rabbitmq.password=ziaant123

# JWT (meme secret que auth-service)
jwt.secret=ZiaantReserveTableCMSuperSecretKeyForJWT2025MustBe256BitsLong!

# Swagger
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
